import { styled } from "../../../stitches.config";

export const FooterContainer = styled('footer', {
  width: '100%',
  display: 'flex',
  flexDirection: 'column',
  background: '$gray90',
  padding: '2rem',
})